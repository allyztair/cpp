#include <iostream>
#include <cmath> //header file

int main()
{

    double x = 3.99;
    double y = 5.56;
    double z;

    // z = std::max(x, y); // maximum
    // z = std::min(x, y); // minimum
    // z = pow(2, 3); // power function
    // z = sqrt(121); // square root
    // z = abs(-3); // absolute value
    // z = round(x); // round of
    // z = ceil(y); // round up
    // z = floor(x); // always round down

    std::cout << z;

    return 0;
}