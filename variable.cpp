#include <iostream>

int main(){

    /* Different datatypes:
        int (whole number) - INTEGER
        double (number including decimal) - DOUBLE
        char (single character) - CHARACTER
        bool (true or false) - BOOLEAM
        str (objects that represents a sequence of text) - STRING
        */

       std::string name = "Nerisse"; // to declare a string
       int age = 23;

    std::cout << "Hello" << " " << name << '\n';
    std::cout << "You are " << age << " years old";

    return 0;
}